name: Paper 1 Liu 2025 ammonia matched validation

on:
  push:
    branches:
      - paper1/multidataset-study-aware-replication
    paths:
      - 'validation_v2/liu2025_ammonia_primary_provenance.py'
      - 'validation_v2/liu2025_ammonia_matched_validation.py'
      - '.github/workflows/paper1-liu2025-ammonia-matched-validation.yml'
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: python -m pip install --upgrade pip pandas openpyxl numpy requests scikit-learn xgboost catboost
      - name: Reconstruct primary-study provenance
        run: python validation_v2/liu2025_ammonia_primary_provenance.py
      - name: Run matched validation
        run: python validation_v2/liu2025_ammonia_matched_validation.py
      - name: Upload deterministic evidence
        uses: actions/upload-artifact@v4
        with:
          name: paper1-liu2025-ammonia-matched-validation
          path: |
            validation_v2/outputs/multidataset/liu2025_ammonia_primary_provenance/
            validation_v2/outputs/multidataset/liu2025_ammonia_matched_validation/
          if-no-files-found: error
